/* ============================================================
   Mavix Web — dashboard.js
   Логика, специфичная для главной кабинета.
   Подстановка email/USER_ID вынесена в общий js/cabinet.js.
   ============================================================ */

(function () {
  // Точка расширения для будущей логики dashboard.
})();
